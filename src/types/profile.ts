export interface PatientProfile {
    first_name: string;
    last_name: string;
    gender: string;
    birth_date: string;
    phone: string;
    address: string;
    snils: string;
}

export interface DoctorProfile {
    
}
